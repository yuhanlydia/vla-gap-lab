# Track 2 — Persistence-Revision

Gate-0: evaluate normal, freeze, and reset-from-initial-memory interventions on native MIKASA static and tracking tasks. This reset is diagnostic, not an oracle: it also erases hidden identity. Continue to mechanism training only if a later identity-preserving location intervention improves tracking by at least 10 percentage points without being required on static tasks.
