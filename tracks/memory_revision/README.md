# Track 2 — Persistence-Revision

Gate-0: evaluate normal, freeze, and oracle-refresh memory interventions on native MIKASA static and tracking tasks. Continue only if oracle refresh improves tracking success by at least 10 percentage points without being required on static tasks.

